import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'beaver-taximetro5',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
